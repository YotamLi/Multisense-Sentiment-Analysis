# Model checkpoint

Place the final V2 checkpoint here:

```text
checkpoints/best_model.pt
```

The checkpoint is intentionally excluded from this code package because it is a large binary file. Publish it through a GitHub Release, Git LFS, or a Hugging Face model repository.
